name: iniz
description: "AI-powered customer assistant for iniz"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^14.2.0
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  flutter_animate: ^4.5.0
  intl: ^0.19.0
  uuid: ^4.4.0
  flutter_markdown: ^0.7.3
  url_launcher: ^6.3.0
  connectivity_plus: ^6.0.3
  provider: ^6.1.0
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
